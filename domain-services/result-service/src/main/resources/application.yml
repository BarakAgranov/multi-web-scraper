server.port: 7004
server.error.include-message: always

logging:
  level:
    root: INFO
    se.magnus.domain-services: DEBUG
---
spring:
  config:
    activate:
      on-profile: docker
server:
  port: 8080